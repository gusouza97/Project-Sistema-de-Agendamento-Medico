import styles from './Input.module.css';

function Input({type, name, text, placeholder, value}){
    return(
        <div>
            <label></label>
            <input></input>
        </div>
    );
}

export default Input;